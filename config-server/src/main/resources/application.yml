server:
  port: ${CONFIG_SERVER_PORT}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_URL}
          default-label: ${CONFIG_LABEL}
          username: ${CONFIG_USER}
          password: ${CONFIG_PASS}
